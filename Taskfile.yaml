version: '3'

set: [errexit, nounset, pipefail]

vars:
  # ===================================================================
  # Variable Naming Conventions
  # ===================================================================
  # - Taskfile variables: UPPER_SNAKE_CASE (e.g., GATEWAY_NS)
  # - Exported for envsubst: lower_snake_case (e.g., gateway_ns)
  # - Template placeholders: ${lower_snake_case} via envsubst
  #
  # Common suffixes:
  #   _TEMPLATE  - Source template file (.tftpl)
  #   _RENDERED  - Output after envsubst rendering
  #   _NS        - Kubernetes namespace
  #   _STAMP     - Idempotence marker file
  #   _VERSION   - Component version
  #   _RELEASE   - Helm release name
  # ===================================================================

  # -------------------------------------------------------------------
  # Cluster topology
  # -------------------------------------------------------------------
  CP0: cp-0
  WORKER_COUNT: '{{default "2" .WORKER_COUNT}}'
  # WORKERS is auto-generated from WORKER_COUNT in tasks; override for custom names
  WORKERS: '{{default "" .WORKERS}}'
  NODES: '{{.CP0}} {{.WORKERS}}'

  # -------------------------------------------------------------------
  # Versions
  # -------------------------------------------------------------------
  K8S_VERSION_FULL: '{{default "1.34.2" .K8S_VERSION_FULL}}'
  CALICO_VERSION: '{{default "v3.31.3" .CALICO_VERSION}}'
  PAUSE_IMAGE: '{{default "registry.k8s.io/pause:3.10" .PAUSE_IMAGE}}'

  # Gateway API CRDs (standard channel)
  # Latest supported v1 as of Gateway API v1.4.1 (Dec 2025). Override if you want.
  GATEWAY_API_VERSION: '{{default "1.4.1" .GATEWAY_API_VERSION}}'

  # NGINX Gateway Fabric (installed via OCI chart; chart version optional)
  GATEWAY_NS: '{{default "nginx-gateway" .GATEWAY_NS}}'
  GATEWAY_RELEASE: '{{default "ngf" .GATEWAY_RELEASE}}'
  GATEWAY_CHART: '{{default "oci://ghcr.io/nginx/charts/nginx-gateway-fabric" .GATEWAY_CHART}}'
  GATEWAY_CHART_VERSION: '{{default "" .GATEWAY_CHART_VERSION}}'

  # NodePort exposure (HTTP is used; HTTPS NodePort is reserved for future TLS)
  GATEWAY_NODEPORT_HTTP: '{{default "30080" .GATEWAY_NODEPORT_HTTP}}'
  GATEWAY_NODEPORT_HTTPS: '{{default "30443" .GATEWAY_NODEPORT_HTTPS}}'

  # The GatewayClass name NGF creates by default.
  GATEWAY_CLASS: '{{default "nginx" .GATEWAY_CLASS}}'

  # A single shared Gateway for the demo
  GATEWAY_NAME: '{{default "demo-gateway" .GATEWAY_NAME}}'

  # local-path-provisioner (dynamic local storage that cleans up on Delete)
  LOCAL_PATH_VERSION: '{{default "v0.0.33" .LOCAL_PATH_VERSION}}'
  LOCAL_PATH_NS: '{{default "local-path-storage" .LOCAL_PATH_NS}}'
  LOCAL_PATH_MANIFEST_URL: 'https://raw.githubusercontent.com/rancher/local-path-provisioner/{{.LOCAL_PATH_VERSION}}/deploy/local-path-storage.yaml'
  LOCAL_PATH_STORAGECLASS: '{{default "local-path" .LOCAL_PATH_STORAGECLASS}}'
  LOCAL_PATH_DIR: '{{default "/opt/local-path-provisioner" .LOCAL_PATH_DIR}}'

  # -------------------------------------------------------------------
  # Networking (single source of truth)
  # -------------------------------------------------------------------
  POD_CIDR: '{{default "10.48.0.0/16" .POD_CIDR}}'
  SERVICE_CIDR: '{{default "10.49.0.0/16" .SERVICE_CIDR}}'
  DNS_DOMAIN: '{{default "cluster.local" .DNS_DOMAIN}}'

  # Demo base domain (host routing)
  EDGE_HOST: '{{default "local.k8s" .EDGE_HOST}}'

  # Derived hostnames
  HEADLAMP_HOST: 'headlamp.{{.EDGE_HOST}}'
  WHISKER_HOST: 'whisker.{{.EDGE_HOST}}'

  # -------------------------------------------------------------------
  # Local state / cloud-init
  # -------------------------------------------------------------------
  # IMPORTANT: Anchor these to the Taskfile directory so STATE_DIR never becomes "/"
  STATE_DIR: '{{.TASKFILE_DIR}}/.state'
  CLOUD_INIT_TEMPLATE: '{{.TASKFILE_DIR}}/cloud-init.tftpl'
  CLOUD_INIT_RENDERED: '{{.STATE_DIR}}/cloud-init.yaml'

  AUDIT_POLICY_TEMPLATE: '{{.TASKFILE_DIR}}/audit-policy.tftpl'
  AUDIT_POLICY_RENDERED: '{{.STATE_DIR}}/audit-policy.yaml'

  KUBEADM_CONFIG_TEMPLATE: '{{.TASKFILE_DIR}}/kubeadm-config.tftpl'
  KUBEADM_CONFIG_RENDERED: '{{.STATE_DIR}}/kubeadm-config.yaml'

  CALICO_CUSTOM_RESOURCES_TEMPLATE: '{{.TASKFILE_DIR}}/calico-custom-resources.tftpl'
  CALICO_CUSTOM_RESOURCES_RENDERED: '{{.STATE_DIR}}/calico-custom-resources.yaml'

  # -------------------------------------------------------------------
  # Rendered manifests (grouped templates)
  # -------------------------------------------------------------------
  HEADLAMP_MANIFEST_TEMPLATE: '{{.TASKFILE_DIR}}/headlamp.tftpl'
  HEADLAMP_MANIFEST_RENDERED: '{{.STATE_DIR}}/headlamp.yaml'

  # Gateway API template (replaces Ingress template)
  GATEWAY_RULES_TEMPLATE: '{{.TASKFILE_DIR}}/gateway.tftpl'
  GATEWAY_RULES_RENDERED: '{{.STATE_DIR}}/gateway.yaml'

  KUBECONFIG_OUT: '{{.STATE_DIR}}/kubeconfig'
  ENV_OUT: '{{.STATE_DIR}}/env'

  SSH_KEY_FILE: '{{default (printf "%s/.ssh/id_rsa.pub" .HOME) .SSH_KEY_FILE}}'

  MP_IMAGE: '{{default "24.04" .MP_IMAGE}}'
  MP_CPUS: '{{default "2" .MP_CPUS}}'
  MP_MEM: '{{default "4G" .MP_MEM}}'
  MP_DISK: '{{default "20G" .MP_DISK}}'

  # -------------------------------------------------------------------
  # Stamp files (idempotence without noisy repeats)
  # -------------------------------------------------------------------
  CALICO_STAMP: '{{.STATE_DIR}}/.calico-{{.CALICO_VERSION}}.installed'
  GATEWAY_STAMP: '{{.STATE_DIR}}/.ngf-{{.GATEWAY_NODEPORT_HTTP}}-{{.GATEWAY_NODEPORT_HTTPS}}.installed'
  LOCAL_PATH_STAMP: '{{.STATE_DIR}}/.local-path-{{.LOCAL_PATH_VERSION}}.installed'
  HEADLAMP_STAMP: '{{.STATE_DIR}}/.headlamp-{{.HEADLAMP_RELEASE}}.installed'

  # -------------------------------------------------------------------
  # Demo components: Headlamp
  # -------------------------------------------------------------------
  HEADLAMP_NS: '{{default "kube-system" .HEADLAMP_NS}}'
  HEADLAMP_RELEASE: '{{default "headlamp" .HEADLAMP_RELEASE}}'
  HEADLAMP_CHART: '{{default "headlamp/headlamp" .HEADLAMP_CHART}}'
  HEADLAMP_CHART_VERSION: '{{default "" .HEADLAMP_CHART_VERSION}}'

  HEADLAMP_SA: '{{default "headlamp-admin" .HEADLAMP_SA}}'
  HEADLAMP_CLUSTERROLE: '{{default "cluster-admin" .HEADLAMP_CLUSTERROLE}}'
  HEADLAMP_TOKEN_DURATION: '{{default "1h" .HEADLAMP_TOKEN_DURATION}}'
  HEADLAMP_SVC_PORT: '{{default "80" .HEADLAMP_SVC_PORT}}'

  # -------------------------------------------------------------------
  # Calico Whisker namespace
  # -------------------------------------------------------------------
  WHISKER_NS: '{{default "calico-system" .WHISKER_NS}}'

tasks:
  # =====================================================================
  # Public tasks
  # =====================================================================

  default:
    desc: 'List available tasks'
    silent: true
    cmds:
      - task --list

  cluster:up:
    desc: 'Create cluster (VMs + kubeadm + kubeconfig + calico) and wait until Ready'
    silent: true
    cmds:
      - task: cluster:calico
      - task: cluster:wait
      - |
        set -euo pipefail
        mkdir -p '{{.STATE_DIR}}'
        cat > '{{.ENV_OUT}}' <<EOF
        # Source this to talk to the cluster created by this repo.
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'
        EOF
        echo "Environment written: {{.ENV_OUT}}"
        echo "Run: source {{.ENV_OUT}}"

  cluster:down:
    desc: 'Delete cluster VMs + local state (keeps Multipass cache)'
    silent: true
    cmds:
      - |
        set -euo pipefail
        # Generate node list from WORKER_COUNT if WORKERS not explicitly set
        if [ -n '{{.WORKERS}}' ]; then
          NODES='{{.CP0}} {{.WORKERS}}'
        else
          NODES='{{.CP0}}'
          for i in $(seq 0 $(({{.WORKER_COUNT}} - 1))); do
            NODES="${NODES} worker-${i}"
          done
        fi

        for n in $NODES; do
          if multipass info "$n" >/dev/null 2>&1; then
            echo "Deleting $n..."
            multipass delete -p "$n" >/dev/null 2>&1 || true
          fi
        done
        rm -rf '{{.STATE_DIR}}'
        echo '✓ cluster destroyed (cache preserved)'
        echo '• To also wipe Multipass cache/images: task cluster:purge'

  cluster:purge:
    desc: 'Delete cluster VMs + local state + Multipass cache (purge)'
    silent: true
    cmds:
      - |
        set -euo pipefail
        # Generate node list from WORKER_COUNT if WORKERS not explicitly set
        if [ -n '{{.WORKERS}}' ]; then
          NODES='{{.CP0}} {{.WORKERS}}'
        else
          NODES='{{.CP0}}'
          for i in $(seq 0 $(({{.WORKER_COUNT}} - 1))); do
            NODES="${NODES} worker-${i}"
          done
        fi

        for n in $NODES; do
          if multipass info "$n" >/dev/null 2>&1; then
            echo "Deleting $n..."
            multipass delete -p "$n" >/dev/null 2>&1 || true
          fi
        done
        echo 'Purging Multipass (removes cached images too)...'
        multipass purge >/dev/null 2>&1 || true
        rm -rf '{{.STATE_DIR}}'
        echo '✓ cluster destroyed + multipass purged'

  cluster:status:
    desc: 'Show cluster nodes'
    silent: true
    cmds:
      - |
        set -euo pipefail
        if ! multipass info '{{.CP0}}' >/dev/null 2>&1; then
          echo 'Cluster is not running.'
          echo 'Run: task cluster:up'
          exit 1
        fi
        if [ ! -s '{{.KUBECONFIG_OUT}}' ]; then
          task cluster:kubeconfig >/dev/null
        fi
        KUBECONFIG='{{.KUBECONFIG_OUT}}' kubectl get nodes -o wide

  demo:install:
    desc: 'Install demo (cluster + storage + Headlamp + Gateway API exposure) and print URLs'
    silent: true
    cmds:
      - task: cluster:up
      - task: storage:local-path:install
      - task: apps:headlamp:install
      - task: apps:gateway:install
      - task: demo:info

  demo:uninstall:
    desc: 'Uninstall demo apps (keeps cluster running)'
    silent: true
    deps: [cluster:ready]
    cmds:
      - task: apps:gateway:uninstall
      - task: apps:headlamp:uninstall
      - |
        echo '✓ demo uninstalled'
        echo '• Cluster still running. To delete VMs/state: task cluster:down'

  demo:info:
    desc: 'Print demo URLs + Headlamp token + /etc/hosts hint'
    silent: true
    deps: [cluster:ready]
    cmds:
      - |
        set -euo pipefail
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'

        echo '✓ demo ready'
        echo
        echo "Base domain: {{.EDGE_HOST}}"
        echo "• Headlamp:   http://{{.HEADLAMP_HOST}}:{{.GATEWAY_NODEPORT_HTTP}}/"
        echo "• Whisker:    http://{{.WHISKER_HOST}}:{{.GATEWAY_NODEPORT_HTTP}}/"
        echo
        echo "Headlamp token (duration: {{.HEADLAMP_TOKEN_DURATION}}):"
        kubectl -n '{{.HEADLAMP_NS}}' create token '{{.HEADLAMP_SA}}' --duration='{{.HEADLAMP_TOKEN_DURATION}}'
        echo
        echo 'Add these to /etc/hosts if the hostnames do not resolve:'
        CP0_IP="$(multipass list --format csv | awk -F, -v n='{{.CP0}}' '$1==n {print $3; exit}')"
        test -n "${CP0_IP:-}"
        echo "${CP0_IP} {{.HEADLAMP_HOST}}"
        echo "${CP0_IP} {{.WHISKER_HOST}}"

  # -------------------------------------------------------------------
  # Cluster readiness helpers
  # -------------------------------------------------------------------

  cluster:wait:
    internal: true
    silent: true
    desc: 'Wait until cluster is usable (API + CNI + nodes Ready)'
    deps: [cluster:ready]
    cmds:
      - |
        set -euo pipefail
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'

        echo 'Waiting for API server responsiveness...'
        for i in $(seq 1 60); do
          if kubectl get nodes >/dev/null 2>&1; then break; fi
          sleep 2
        done
        kubectl get nodes >/dev/null 2>&1 || { echo 'API server not responsive' >&2; exit 1; }

        echo 'Waiting for all nodes to be Ready...'
        kubectl wait node --all --for=condition=Ready --timeout=360s >/dev/null

        echo 'Waiting for kube-system pods to settle...'
        kubectl -n kube-system wait --for=condition=Available deploy/coredns --timeout=240s >/dev/null 2>&1 || true
        kubectl -n kube-system rollout status ds/kube-proxy --timeout=240s >/dev/null 2>&1 || true

        echo '✓ cluster ready'
        kubectl get nodes -o wide

  cluster:ready:
    internal: true
    silent: true
    desc: 'Fail fast if cluster is not reachable'
    cmds:
      - |
        set -euo pipefail
        if ! multipass info '{{.CP0}}' >/dev/null 2>&1; then
          echo 'Cluster is not running (missing multipass instance {{.CP0}}).'
          echo 'Run: task cluster:up'
          exit 1
        fi
        if [ ! -s '{{.KUBECONFIG_OUT}}' ]; then
          task cluster:kubeconfig >/dev/null
        fi
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'
        kubectl get nodes >/dev/null 2>&1

  # -------------------------------------------------------------------
  # Storage: Rancher local-path-provisioner
  # -------------------------------------------------------------------

  storage:local-path:node-dir:
    internal: true
    silent: true
    desc: 'Ensure local-path provisioner node directory exists on all nodes'
    cmds:
      - |
        set -euo pipefail
        # Generate node list from WORKER_COUNT if WORKERS not explicitly set
        if [ -n '{{.WORKERS}}' ]; then
          NODES='{{.CP0}} {{.WORKERS}}'
        else
          NODES='{{.CP0}}'
          for i in $(seq 0 $(({{.WORKER_COUNT}} - 1))); do
            NODES="${NODES} worker-${i}"
          done
        fi

        for n in $NODES; do
          echo "Ensuring {{.LOCAL_PATH_DIR}} on ${n}..."
          multipass exec "$n" -- bash -lc '
            set -euo pipefail
            sudo mkdir -p "{{.LOCAL_PATH_DIR}}"
            sudo chmod 0755 "{{.LOCAL_PATH_DIR}}"
          '
        done

  storage:local-path:install:
    internal: true
    silent: true
    desc: 'Install Rancher local-path-provisioner and set default StorageClass'
    deps: [cluster:ready]
    status:
      - test -f '{{.LOCAL_PATH_STAMP}}'
    cmds:
      - |
        set -euo pipefail
        test -n '{{.STATE_DIR}}'
        test '{{.STATE_DIR}}' != '/'
        mkdir -p '{{.STATE_DIR}}'
      - task: storage:local-path:node-dir
      - |
        set -euo pipefail
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'

        echo "Applying local-path-provisioner: {{.LOCAL_PATH_MANIFEST_URL}}"
        kubectl apply -f '{{.LOCAL_PATH_MANIFEST_URL}}' >/dev/null

        echo "Waiting for local-path-provisioner deployment..."
        kubectl -n '{{.LOCAL_PATH_NS}}' wait --for=condition=Available deploy/local-path-provisioner --timeout=240s >/dev/null

        if ! kubectl get storageclass '{{.LOCAL_PATH_STORAGECLASS}}' >/dev/null 2>&1; then
          echo "Expected StorageClass '{{.LOCAL_PATH_STORAGECLASS}}' not found. Existing storageclasses:"
          kubectl get storageclass
          exit 1
        fi

        for sc in $(kubectl get storageclass -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'); do
          if [ "$sc" != '{{.LOCAL_PATH_STORAGECLASS}}' ]; then
            kubectl patch storageclass "$sc" -p '{"metadata":{"annotations":{"storageclass.kubernetes.io/is-default-class":"false"}}}' >/dev/null 2>&1 \
              || echo "WARNING: failed to unset default on storageclass $sc" >&2
          fi
        done
        kubectl patch storageclass '{{.LOCAL_PATH_STORAGECLASS}}' -p '{"metadata":{"annotations":{"storageclass.kubernetes.io/is-default-class":"true"}}}' >/dev/null 2>&1 \
          || echo "WARNING: failed to set {{.LOCAL_PATH_STORAGECLASS}} as default storageclass" >&2

        touch '{{.LOCAL_PATH_STAMP}}'
        echo "✓ local-path-provisioner ready (default StorageClass: {{.LOCAL_PATH_STORAGECLASS}})"

  # -------------------------------------------------------------------
  # Apps (kept internal; demo:* drives them)
  # -------------------------------------------------------------------

  apps:gateway:install:
    internal: true
    silent: true
    desc: 'Install Gateway API (CRDs + NGF) and apply Gateway/HTTPRoutes'
    deps: [cluster:ready]
    cmds:
      - task: gateway:controller:install
      - task: gateway:routes:apply
      - echo '✓ gateway installed (controller + routes)'

  apps:gateway:uninstall:
    internal: true
    silent: true
    desc: 'Remove Gateway/HTTPRoutes + uninstall NGF (best-effort)'
    deps: [cluster:ready]
    cmds:
      - task: gateway:routes:uninstall
      - |
        set -euo pipefail
        command -v helm >/dev/null 2>&1 || { echo 'missing dependency: helm' >&2; exit 1; }
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'
        helm uninstall '{{.GATEWAY_RELEASE}}' -n '{{.GATEWAY_NS}}' >/dev/null 2>&1 || true
        kubectl delete ns '{{.GATEWAY_NS}}' >/dev/null 2>&1 || true
        rm -f '{{.GATEWAY_STAMP}}' >/dev/null 2>&1 || true
        echo '✓ gateway removed (best-effort)'

  apps:headlamp:install:
    internal: true
    silent: true
    desc: 'Install Headlamp (Helm) + ServiceAccount token auth (waits until Ready)'
    deps: [cluster:ready, cluster:render]
    status:
      - test -f '{{.HEADLAMP_STAMP}}'
    cmds:
      - |
        set -euo pipefail
        command -v helm >/dev/null 2>&1 || { echo 'missing dependency: helm' >&2; exit 1; }
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'

        helm repo add headlamp https://kubernetes-sigs.github.io/headlamp/ >/dev/null 2>&1 \
          || echo 'WARNING: helm repo add failed (may already exist or network issue)' >&2
        helm repo update >/dev/null 2>&1 \
          || echo 'WARNING: helm repo update failed (network issue or no repos configured)' >&2

        kubectl get ns '{{.HEADLAMP_NS}}' >/dev/null 2>&1 || kubectl create ns '{{.HEADLAMP_NS}}'

        if [ -n '{{.HEADLAMP_CHART_VERSION}}' ]; then
          helm upgrade --install '{{.HEADLAMP_RELEASE}}' '{{.HEADLAMP_CHART}}' \
            -n '{{.HEADLAMP_NS}}' --version '{{.HEADLAMP_CHART_VERSION}}'
        else
          helm upgrade --install '{{.HEADLAMP_RELEASE}}' '{{.HEADLAMP_CHART}}' \
            -n '{{.HEADLAMP_NS}}'
        fi

        kubectl -n '{{.HEADLAMP_NS}}' get sa '{{.HEADLAMP_SA}}' >/dev/null 2>&1 || \
          kubectl -n '{{.HEADLAMP_NS}}' create serviceaccount '{{.HEADLAMP_SA}}' >/dev/null

        kubectl apply -f '{{.HEADLAMP_MANIFEST_RENDERED}}' >/dev/null

        echo 'Waiting for Headlamp deployments to become Available...'
        kubectl -n '{{.HEADLAMP_NS}}' wait --for=condition=Available deploy -l app.kubernetes.io/instance='{{.HEADLAMP_RELEASE}}' --timeout=300s >/dev/null 2>&1 || true
        kubectl -n '{{.HEADLAMP_NS}}' wait --for=condition=Available deploy/'{{.HEADLAMP_RELEASE}}' --timeout=300s >/dev/null 2>&1 || true

        mkdir -p '{{.STATE_DIR}}'
        touch '{{.HEADLAMP_STAMP}}'

        echo '✓ headlamp ready'
        kubectl -n '{{.HEADLAMP_NS}}' get pods -o wide | grep -i headlamp || true

  apps:headlamp:uninstall:
    internal: true
    silent: true
    desc: 'Uninstall Headlamp + SA/CRB (best-effort)'
    deps: [cluster:ready]
    cmds:
      - |
        set -euo pipefail
        command -v helm >/dev/null 2>&1 || { echo 'missing dependency: helm' >&2; exit 1; }
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'

        CRB_NAME='{{.HEADLAMP_SA}}-{{.HEADLAMP_CLUSTERROLE}}'
        helm uninstall '{{.HEADLAMP_RELEASE}}' -n '{{.HEADLAMP_NS}}' >/dev/null 2>&1 || true
        kubectl delete clusterrolebinding "${CRB_NAME}" >/dev/null 2>&1 || true
        kubectl -n '{{.HEADLAMP_NS}}' delete sa '{{.HEADLAMP_SA}}' >/dev/null 2>&1 || true
        rm -f '{{.HEADLAMP_STAMP}}' >/dev/null 2>&1 || true
        echo '✓ headlamp removed (best-effort)'

  # -------------------------------------------------------------------
  # Cluster build plumbing
  # -------------------------------------------------------------------

  cluster:preflight:
    silent: true
    desc: 'Preflight checks'
    cmds:
      - |
        set -euo pipefail
        need() { command -v "$1" >/dev/null 2>&1 || { echo "missing dependency: $1" >&2; exit 1; }; }
        need multipass
        need kubectl
        need helm
        need awk
        need sed
        need grep
        need head
        need envsubst
        test -f '{{.CLOUD_INIT_TEMPLATE}}' || { echo "missing: {{.CLOUD_INIT_TEMPLATE}}" >&2; exit 1; }
        test -f '{{.AUDIT_POLICY_TEMPLATE}}' || { echo "missing: {{.AUDIT_POLICY_TEMPLATE}}" >&2; exit 1; }
        test -f '{{.KUBEADM_CONFIG_TEMPLATE}}' || { echo "missing: {{.KUBEADM_CONFIG_TEMPLATE}}" >&2; exit 1; }
        test -f '{{.CALICO_CUSTOM_RESOURCES_TEMPLATE}}' || { echo "missing: {{.CALICO_CUSTOM_RESOURCES_TEMPLATE}}" >&2; exit 1; }
        test -f '{{.HEADLAMP_MANIFEST_TEMPLATE}}' || { echo "missing: {{.HEADLAMP_MANIFEST_TEMPLATE}}" >&2; exit 1; }
        test -f '{{.GATEWAY_RULES_TEMPLATE}}' || { echo "missing: {{.GATEWAY_RULES_TEMPLATE}}" >&2; exit 1; }

  cluster:render:
    internal: true
    silent: true
    desc: 'Render templates to .state/'
    deps: [cluster:preflight]
    cmds:
      - |
        set -euo pipefail
        mkdir -p '{{.STATE_DIR}}'

        KEY_FILE='{{.SSH_KEY_FILE}}'
        if [ ! -f "$KEY_FILE" ]; then
          for cand in "$HOME/.ssh/id_ed25519.pub" "$HOME/.ssh/id_rsa.pub" "$HOME/.ssh/id_ecdsa.pub"; do
            if [ -f "$cand" ]; then KEY_FILE="$cand"; break; fi
          done
        fi
        test -f "$KEY_FILE" || { echo "missing SSH public key (set SSH_KEY_FILE=...)" >&2; exit 1; }

        SSH_KEY="$(cat "$KEY_FILE")"
        K8S_MINOR='{{.K8S_VERSION_FULL}}'
        K8S_MINOR="${K8S_MINOR%.*}"

        export kubernetes_version_full='{{.K8S_VERSION_FULL}}'
        export kubernetes_version="$K8S_MINOR"
        export pod_cidr='{{.POD_CIDR}}'
        export service_cidr='{{.SERVICE_CIDR}}'
        export dns_domain='{{.DNS_DOMAIN}}'

        cp '{{.AUDIT_POLICY_TEMPLATE}}' '{{.AUDIT_POLICY_RENDERED}}'
        test -s '{{.AUDIT_POLICY_RENDERED}}' || { echo "render failed: empty {{.AUDIT_POLICY_RENDERED}}" >&2; exit 1; }
        chmod 0644 '{{.AUDIT_POLICY_RENDERED}}'

        envsubst \
          '${kubernetes_version_full} ${service_cidr} ${pod_cidr} ${dns_domain}' \
          < '{{.KUBEADM_CONFIG_TEMPLATE}}' \
          > '{{.KUBEADM_CONFIG_RENDERED}}'
        test -s '{{.KUBEADM_CONFIG_RENDERED}}' || { echo "render failed: empty {{.KUBEADM_CONFIG_RENDERED}}" >&2; exit 1; }
        chmod 0644 '{{.KUBEADM_CONFIG_RENDERED}}'

        envsubst \
          '${pod_cidr}' \
          < '{{.CALICO_CUSTOM_RESOURCES_TEMPLATE}}' \
          > '{{.CALICO_CUSTOM_RESOURCES_RENDERED}}'
        test -s '{{.CALICO_CUSTOM_RESOURCES_RENDERED}}' || { echo "render failed: empty {{.CALICO_CUSTOM_RESOURCES_RENDERED}}" >&2; exit 1; }
        chmod 0644 '{{.CALICO_CUSTOM_RESOURCES_RENDERED}}'

        export headlamp_ns='{{.HEADLAMP_NS}}'
        export headlamp_sa='{{.HEADLAMP_SA}}'
        export headlamp_clusterrole='{{.HEADLAMP_CLUSTERROLE}}'
        export crb_name='{{.HEADLAMP_SA}}-{{.HEADLAMP_CLUSTERROLE}}'

        export whisker_ns='{{.WHISKER_NS}}'
        export headlamp_host='{{.HEADLAMP_HOST}}'
        export whisker_host='{{.WHISKER_HOST}}'

        export gateway_ns='{{.GATEWAY_NS}}'
        export gateway_name='{{.GATEWAY_NAME}}'
        export gateway_class='{{.GATEWAY_CLASS}}'

        HEADLAMP_CHART_NAME='headlamp'
        if echo '{{.HEADLAMP_RELEASE}}' | grep -q "$HEADLAMP_CHART_NAME"; then
          export headlamp_svc='{{.HEADLAMP_RELEASE}}'
        else
          export headlamp_svc='{{.HEADLAMP_RELEASE}}-headlamp'
        fi
        export headlamp_svc_port='{{.HEADLAMP_SVC_PORT}}'

        envsubst \
          '${crb_name} ${headlamp_clusterrole} ${headlamp_sa} ${headlamp_ns}' \
          < '{{.HEADLAMP_MANIFEST_TEMPLATE}}' \
          > '{{.HEADLAMP_MANIFEST_RENDERED}}'
        test -s '{{.HEADLAMP_MANIFEST_RENDERED}}' || { echo "render failed: empty {{.HEADLAMP_MANIFEST_RENDERED}}" >&2; exit 1; }
        chmod 0644 '{{.HEADLAMP_MANIFEST_RENDERED}}'

        envsubst \
          '${gateway_name} ${gateway_ns} ${gateway_class} ${headlamp_ns} ${whisker_ns} ${headlamp_host} ${whisker_host} ${headlamp_svc} ${headlamp_svc_port}' \
          < '{{.GATEWAY_RULES_TEMPLATE}}' \
          > '{{.GATEWAY_RULES_RENDERED}}'
        test -s '{{.GATEWAY_RULES_RENDERED}}' || { echo "render failed: empty {{.GATEWAY_RULES_RENDERED}}" >&2; exit 1; }
        chmod 0644 '{{.GATEWAY_RULES_RENDERED}}'

        audit_policy="$(sed '1!s/^/      /' '{{.AUDIT_POLICY_RENDERED}}')"
        kubeadm_config="$(sed '1!s/^/      /' '{{.KUBEADM_CONFIG_RENDERED}}')"

        export ssh_key="$SSH_KEY"
        export audit_policy
        export kubeadm_config
        export pause_image='{{.PAUSE_IMAGE}}'

        envsubst \
          '${ssh_key} ${kubernetes_version_full} ${kubernetes_version} ${pod_cidr} ${service_cidr} ${dns_domain} ${audit_policy} ${kubeadm_config} ${pause_image}' \
          < '{{.CLOUD_INIT_TEMPLATE}}' \
          > '{{.CLOUD_INIT_RENDERED}}'

        test -s '{{.CLOUD_INIT_RENDERED}}' || { echo "render failed: empty {{.CLOUD_INIT_RENDERED}}" >&2; exit 1; }
        head -n1 '{{.CLOUD_INIT_RENDERED}}' | grep -q '^#cloud-config' || { echo "render sanity check failed (first line must be #cloud-config)" >&2; exit 1; }

        chmod 0644 '{{.CLOUD_INIT_RENDERED}}'
        echo "Rendered: {{.CLOUD_INIT_RENDERED}}"
        echo "Rendered: {{.AUDIT_POLICY_RENDERED}}"
        echo "Rendered: {{.KUBEADM_CONFIG_RENDERED}}"
        echo "Rendered: {{.CALICO_CUSTOM_RESOURCES_RENDERED}}"
        echo "Rendered: {{.HEADLAMP_MANIFEST_RENDERED}}"
        echo "Rendered: {{.GATEWAY_RULES_RENDERED}}"
        echo "Using SSH key: ${KEY_FILE}"

  cluster:vms:
    internal: true
    silent: true
    desc: 'Launch multipass VMs (idempotent)'
    deps: [cluster:render]
    cmds:
      - |
        set -euo pipefail
        CLOUD='{{.CLOUD_INIT_RENDERED}}'
        test -s "$CLOUD" || { echo "missing cloud-init: $CLOUD" >&2; exit 1; }

        launch() {
          local name="$1"
          if multipass info "$name" >/dev/null 2>&1; then
            echo "✓ exists: $name"
            return 0
          fi

          multipass launch \
            --name "$name" \
            --memory '{{.MP_MEM}}' \
            --disk '{{.MP_DISK}}' \
            --cpus '{{.MP_CPUS}}' \
            --cloud-init "$CLOUD" \
            '{{.MP_IMAGE}}' >/dev/null

          echo "✓ launched: $name"
        }

        # Generate worker names from WORKER_COUNT if WORKERS not explicitly set
        if [ -n '{{.WORKERS}}' ]; then
          WORKERS='{{.WORKERS}}'
        else
          WORKERS=''
          for i in $(seq 0 $(({{.WORKER_COUNT}} - 1))); do
            WORKERS="${WORKERS} worker-${i}"
          done
        fi

        echo 'Launching VMs (idempotent)...'
        launch '{{.CP0}}'
        for w in $WORKERS; do
          launch "$w"
        done
        multipass list

  cluster:init:
    internal: true
    silent: true
    desc: 'kubeadm init (idempotent)'
    deps: [cluster:vms]
    status:
      - multipass exec '{{.CP0}}' -- bash -lc 'test -f /etc/kubernetes/admin.conf' >/dev/null 2>&1
    cmds:
      - |
        set -euo pipefail
        echo 'Initializing control-plane on {{.CP0}}...'
        multipass exec '{{.CP0}}' -- bash -lc '
          set -euo pipefail
          sudo kubeadm init --config /etc/kubernetes/kubeadm-config.yaml
          echo "✓ initialized"
        '

  cluster:join:
    internal: true
    silent: true
    desc: 'Join workers (idempotent)'
    deps: [cluster:init]
    cmds:
      - |
        set -euo pipefail
        echo 'Creating join command ({{.CP0}})...'
        JOIN_BASE="$(multipass exec '{{.CP0}}' -- bash -lc 'sudo kubeadm token create --print-join-command')"

        joined_worker() {
          local name="$1"
          multipass exec "$name" -- bash -lc \
            'test -f /etc/kubernetes/kubelet.conf && test -f /etc/kubernetes/pki/ca.crt' \
            >/dev/null 2>&1
        }

        # Generate worker names from WORKER_COUNT if WORKERS not explicitly set
        if [ -n '{{.WORKERS}}' ]; then
          WORKERS='{{.WORKERS}}'
        else
          WORKERS=''
          for i in $(seq 0 $(({{.WORKER_COUNT}} - 1))); do
            WORKERS="${WORKERS} worker-${i}"
          done
        fi

        for w in $WORKERS; do
          if joined_worker "$w"; then
            echo "✓ already joined: $w"
            continue
          fi
          echo "Joining worker $w..."
          multipass exec "$w" -- bash -lc "sudo ${JOIN_BASE} --cri-socket unix:///run/containerd/containerd.sock"
          echo "✓ joined: $w"
        done

  cluster:kubeconfig:
    internal: true
    silent: true
    desc: 'Write kubeconfig to .state/kubeconfig'
    deps: [cluster:join]
    cmds:
      - |
        set -euo pipefail
        mkdir -p '{{.STATE_DIR}}'

        CP0_IP="$(multipass list --format csv | awk -F, -v n='{{.CP0}}' '$1==n {print $3; exit}')"
        test -n "${CP0_IP:-}"

        multipass exec '{{.CP0}}' -- bash -lc 'sudo cat /etc/kubernetes/admin.conf' \
          | sed -E "s#server: https://[^:]+:6443#server: https://${CP0_IP}:6443#" \
          > '{{.KUBECONFIG_OUT}}'

        chmod 0600 '{{.KUBECONFIG_OUT}}'
        echo "Wrote kubeconfig: {{.KUBECONFIG_OUT}}"

  cluster:calico:
    internal: true
    silent: true
    desc: 'Install Calico (Tigera operator) (idempotent via stamp)'
    deps: [cluster:kubeconfig]
    status:
      - test -f '{{.CALICO_STAMP}}'
    cmds:
      - |
        set -euo pipefail
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'

        kubectl get ns tigera-operator >/dev/null 2>&1 || kubectl create ns tigera-operator

        echo 'Applying Tigera operator CRDs...'
        kubectl apply --server-side --force-conflicts \
          -f 'https://raw.githubusercontent.com/projectcalico/calico/{{.CALICO_VERSION}}/manifests/operator-crds.yaml'

        echo 'Applying Tigera operator...'
        kubectl apply --server-side --force-conflicts \
          -f 'https://raw.githubusercontent.com/projectcalico/calico/{{.CALICO_VERSION}}/manifests/tigera-operator.yaml'

        echo 'Waiting for tigera-operator deployment...'
        kubectl -n tigera-operator wait --for=condition=Available deployment/tigera-operator --timeout=240s >/dev/null

        echo 'Applying Calico custom resources (rendered)...'
        kubectl apply --server-side --force-conflicts -f '{{.CALICO_CUSTOM_RESOURCES_RENDERED}}' >/dev/null

        echo 'Waiting for calico-system namespace...'
        for i in $(seq 1 90); do
          if kubectl get ns calico-system >/dev/null 2>&1; then break; fi
          if [ $((i % 10)) -eq 0 ]; then echo "  still waiting for calico-system namespace... (${i}s)"; fi
          sleep 2
        done
        kubectl get ns calico-system >/dev/null 2>&1 || { echo 'calico-system namespace not created' >&2; exit 1; }

        echo 'Waiting for calico-node DaemonSet to be created...'
        DS_NAME=''
        for i in $(seq 1 120); do
          DS_NAME="$(kubectl -n calico-system get ds 2>/dev/null | awk 'NR>1 {print $1}' | grep -E '^calico-node' | head -n1 || true)"
          if [ -n "${DS_NAME:-}" ]; then break; fi
          if [ $((i % 10)) -eq 0 ]; then echo "  still waiting for calico-node DaemonSet... ($((i*2))s)"; fi
          sleep 2
        done

        if [ -z "${DS_NAME:-}" ]; then
          echo 'calico-node DaemonSet still not present. Debug dump:' >&2
          echo '--- tigera-operator pods ---' >&2
          kubectl -n tigera-operator get pods -o wide >&2 || true
          echo '--- calico-system pods (if any) ---' >&2
          kubectl -n calico-system get pods -o wide >&2 || true
          echo '--- calico-system daemonsets ---' >&2
          kubectl -n calico-system get ds >&2 || true
          echo '--- tigera-operator logs (tail) ---' >&2
          kubectl -n tigera-operator logs deploy/tigera-operator --tail=200 >&2 || true
          exit 1
        fi

        echo "Waiting for ${DS_NAME} rollout..."
        kubectl -n calico-system rollout status "ds/${DS_NAME}" --timeout=600s >/dev/null
        kubectl -n calico-system rollout status deploy/calico-kube-controllers --timeout=600s >/dev/null 2>&1 || true

        touch '{{.CALICO_STAMP}}'
        echo '✓ calico ready'

  # -------------------------------------------------------------------
  # Gateway API (CRDs + NGF controller) + routes
  # -------------------------------------------------------------------

  gateway:crds:install:
    internal: true
    silent: true
    desc: 'Install Gateway API CRDs (standard channel)'
    deps: [cluster:ready]
    cmds:
      - |
        set -euo pipefail
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'
        kubectl apply --server-side=true \
          -f "https://github.com/kubernetes-sigs/gateway-api/releases/download/v{{.GATEWAY_API_VERSION}}/standard-install.yaml" >/dev/null
        echo "✓ gateway-api CRDs installed (v{{.GATEWAY_API_VERSION}})"

  gateway:controller:install:
    internal: true
    silent: true
    desc: 'Install NGINX Gateway Fabric (NodePort) (idempotent via stamp)'
    deps: [cluster:ready, gateway:crds:install]
    status:
      - test -f '{{.GATEWAY_STAMP}}'
    cmds:
      - |
        set -euo pipefail
        command -v helm >/dev/null 2>&1 || { echo 'missing dependency: helm' >&2; exit 1; }
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'

        # Install NGF from OCI. Default chart creates LB Service; we force NodePort.
        if [ -n '{{.GATEWAY_CHART_VERSION}}' ]; then
          helm upgrade --install '{{.GATEWAY_RELEASE}}' '{{.GATEWAY_CHART}}' \
            -n '{{.GATEWAY_NS}}' --create-namespace \
            --version '{{.GATEWAY_CHART_VERSION}}' \
            --set nginx.service.type=NodePort \
            --set nginx.service.externalTrafficPolicy=Cluster \
            --set-json "nginx.service.nodePorts=[{\"port\":{{.GATEWAY_NODEPORT_HTTP}},\"listenerPort\":80},{\"port\":{{.GATEWAY_NODEPORT_HTTPS}},\"listenerPort\":443}]"
        else
          helm upgrade --install '{{.GATEWAY_RELEASE}}' '{{.GATEWAY_CHART}}' \
            -n '{{.GATEWAY_NS}}' --create-namespace \
            --set nginx.service.type=NodePort \
            --set nginx.service.externalTrafficPolicy=Cluster \
            --set-json "nginx.service.nodePorts=[{\"port\":{{.GATEWAY_NODEPORT_HTTP}},\"listenerPort\":80},{\"port\":{{.GATEWAY_NODEPORT_HTTPS}},\"listenerPort\":443}]"
        fi

        echo 'Waiting for NGF deployment to be Available...'
        kubectl -n '{{.GATEWAY_NS}}' wait --for=condition=Available deploy -l app.kubernetes.io/instance='{{.GATEWAY_RELEASE}}' --timeout=240s >/dev/null 2>&1 || true

        mkdir -p '{{.STATE_DIR}}'
        touch '{{.GATEWAY_STAMP}}'

        echo '✓ nginx gateway fabric ready'
        kubectl -n '{{.GATEWAY_NS}}' get pods,svc -o wide

  gateway:routes:apply:
    internal: true
    silent: true
    desc: 'Apply Gateway + HTTPRoutes (from rendered .state/gateway.yaml)'
    deps: [cluster:ready, cluster:render, gateway:controller:install]
    cmds:
      - |
        set -euo pipefail
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'
        kubectl apply -f '{{.GATEWAY_RULES_RENDERED}}' >/dev/null
        echo '✓ gateway routes applied'

  gateway:routes:uninstall:
    internal: true
    silent: true
    desc: 'Uninstall Gateway + HTTPRoutes (best-effort)'
    deps: [cluster:ready]
    cmds:
      - |
        set -euo pipefail
        export KUBECONFIG='{{.KUBECONFIG_OUT}}'
        kubectl -n '{{.HEADLAMP_NS}}' delete httproute headlamp >/dev/null 2>&1 || true
        kubectl -n '{{.WHISKER_NS}}' delete httproute whisker >/dev/null 2>&1 || true
        kubectl -n '{{.GATEWAY_NS}}' delete gateway '{{.GATEWAY_NAME}}' >/dev/null 2>&1 || true
        kubectl -n '{{.WHISKER_NS}}' delete networkpolicy whisker-gateway-allow >/dev/null 2>&1 || true
        echo '✓ gateway routes deleted (best-effort)'

